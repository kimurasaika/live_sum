# PROGRESS.md — Task Log

## Completed Tasks
| Task | Verification | Key Files Changed |
|------|-------------|-------------------|
| Benchmark ASR model candidates (speed/accuracy/realtime) for CPU 2-6 core | `python scripts/bench_asr.py` exits 0. Tested 20+ ct2 repos + 6 streaming-native (NeMo/Typhoon) repos on 5-sample Thai testset (`data/testset/`, gTTS-generated, 33.77s total audio). RESULT: min-max winner = `Systran/faster-whisper-medium` (accuracy 0.70, RTF 0.81, score 0.690) — best balance. Realtime-capable set (RTF<1) with usable accuracy: medium(0.70)/small(0.30)/base(0.20). `faster-distil-whisper-*` = 0.0 accuracy (English-only distillation, unusable for Thai). All streaming-native models (nvidia nemotron, Typhoon ASR x3) fail to load — NeMo format, not ct2-compatible with `faster_whisper.WhisperModel`. RECOMMENDATION (not yet applied): swap `backend/asr.py` default from `small` to `medium`, pending real-audio + beam_size=1 verification. | scripts/bench_asr.py, scripts/gen_testset.py, data/testset/*.mp3, data/testset/manifest.json |
| Spike: NeMo loader for Typhoon streaming ASR — verify CPU realtime capability | FULL PASS on short sample + real 10-min clip both run. `python scripts/bench_typhoon_nemo.py` exit 0 both times. Model `typhoon-ai/typhoon-asr-streaming-115m` loaded via `nemo.collections.asr.models.ASRModel.from_pretrained()` (correct API, not WhisperModel — prior attempts failed because wrong loader was used). SHORT SAMPLE (`test_sample_th.mp3`, 6.26s): LOAD_TIME 30.93s, TRANSCRIBE_TIME 0.57s, **RTF 0.090**, 4/4 expected words exact match, PASS. REAL 10-MIN CLIP (`data/clip.wav`, same audio used in earlier README pipeline run): LOAD_TIME 16.21s, TRANSCRIBE_TIME 223.33s, **RTF 0.372** — still realtime-capable (<1), ~2x faster than Whisper `medium`'s RTF 0.81 on this workload class. TRANSCRIPT QUALITY: markedly better than the `small`-model README run — coherent, readable, correctly on-topic (skincare/สิว/เลเซอร์/โทนเนอร์), no garbled Chinese/Russian chars or English nonsense phrases that plagued the small-model result. ONE DEFECT FOUND: mid-transcript repetition loop, `"มันจะมันจะมันจะ..."` repeated ~20x consecutively (RNNT decoder stuck/hallucination) — isolated single occurrence in 10 min, doesn't destroy overall transcript, but is a known failure mode to watch for. `nemo_toolkit[asr]` installed (~50 new packages); confirmed does NOT break faster-whisper/torch stack (`test_asr.py` still runs post-install). One dependency conflict noted (gtts wants click<8.2, got 8.4.2 — not yet broken anything in practice, gtts itself not re-tested). Model is RNNT (EncDecRNNTBPEModel) architecture. **This is now the strongest ASR candidate found in this project** — faster AND cleaner output than any faster-whisper size tested on real noisy audio. | scripts/bench_typhoon_nemo.py, requirements.txt (nemo_toolkit not yet added, pending decision) |
| Project initialized — Ayanokoji scaffold created | All four scaffold files generated | AGENTS.md, CONTEXT.md, TASK.md, PROGRESS.md |
| ASR pipeline built + verified (backend + frontend) | FULL PASS: `python test_asr.py` -> `TRANSCRIPT: 'สวัสดีครับ วันนี้เราจะมาประชุมเรื่องงบประมาณของบริษัท'` / `MATCHED: ['สวัสดี', 'ประชุม', 'งบประมาณ', 'บริษัท']` / `PASS: transcript matches expected Thai sentence`. Model: large-v3, int8, cpu (small and medium both failed accuracy bar, see Failed Attempts). `uvicorn backend.main:app` boots clean. | backend/main.py, backend/asr.py, backend/__init__.py, frontend/index.html, frontend/app.js, requirements.txt, test_asr.py |
| YouTube 10-min clip -> README.md summary | PASS (acceptance criteria met, quality caveat below): `python scripts/run_pipeline.py` exited 0, `README.md` created, non-empty, Thai text. `TRANSCRIPT_LENGTH: 1822` chars from a 10-min real segment (VAD likely dropped long silence/music). QUALITY CAVEAT: transcript is rough — real casual live-stream speech with code-switching (Thai/English loanwords) confused the "small" model, producing garbled fragments (stray Chinese/Russian characters, nonsense English phrases like "cannon serpent"). mT5 summary line is barely coherent. Topic is skincare (สิว, A-Strip, Moisturizer, โทนเนอร์) — correctly identified topic despite noisy transcript. Model had to be downgraded large-v3 -> medium -> small purely for speed (see Failed Attempts); this task never got to test whether a bigger model would also improve quality on real noisy audio, only that big models were too slow on this CPU for a 10-min real clip. | scripts/fetch_youtube_clip.py, scripts/run_pipeline.py, backend/summarize.py, backend/asr.py (model reverted to small — flagged deviation), requirements.txt, README.md |

## Failed Attempts
- `python test_asr.py` crashed with `UnicodeEncodeError: 'charmap' codec can't encode characters` — Windows console cp1252 can't print Thai text. Fixed by wrapping stdout in `io.TextIOWrapper(..., encoding="utf-8")`. Never print raw unicode transcript to default stdout on Windows without this wrapper.
- large-v3 download crashed: `OSError: [WinError 1314] A required privilege is not held by the client` on `os.symlink` — Windows needs Developer Mode or admin for HF hub symlink cache. Fix: set env var `HF_HUB_DISABLE_SYMLINKS=1` before download so huggingface_hub copies files instead of symlinking. Never assume HF cache symlinks work on plain Windows.
- `deepdml/faster-whisper-large-v3-turbo-ct2` load crashed: `OSError: [WinError 1314] A required privilege is not held by the client` on blob->snapshot link for `tokenizer.json`, even with `HF_HUB_DISABLE_SYMLINKS=1` set. Disabling symlinks does not fully fix all HF cache link ops on Windows for every repo — some repos still hit this on specific files. Never assume `HF_HUB_DISABLE_SYMLINKS=1` alone guarantees a clean download on Windows; may need per-repo retry or manual cache clear.
- `Qwen/Qwen3-ASR-0.6B-hf` load crashed: `RuntimeError: Unable to open file 'model.bin'` — repo is a native transformers checkpoint, not CTranslate2/ct2 format, incompatible with `faster_whisper.WhisperModel`. Never load non-ct2 repos via `WhisperModel` directly; would need a separate `transformers`-based loader, out of scope for faster-whisper-based bench.
- Benchmarked 4 official ct2 models (tiny/base/small/medium) on `test_sample_th.mp3` — **none passed** the `test_asr.py` threshold (>=3/4 expected words). Best: `base` and `small` both matched 2/4, `base` 2.6x faster. Single short sample may be too strict/unrepresentative for model comparison; do not conclude "no usable model" from this alone — need to try Thai-specific fine-tuned models next (generic multilingual Whisper may just be weak on this exact sample).
- `biodatlab/distill-whisper-th-small`, `biodatlab/distill-whisper-th-medium`, `biodatlab/whisper-th-small` (Thai-specific fine-tunes, real org, 1.6k+ downloads) all **failed to load** via `faster_whisper.WhisperModel` — these are native transformers/HF checkpoints (`model.bin`/`pytorch_model.bin` + `config.json`), NOT CTranslate2-converted. `distill-whisper-th-small` additionally hit the same Windows symlink `WinError 1314` on `config.json` even with `HF_HUB_DISABLE_SYMLINKS=1` set. To actually benchmark these, would need `ct2-transformers-converter` (from `ctranslate2` package) to convert the checkpoint to ct2 format first — extra one-time step, not yet done, out of scope for `scripts/bench_asr.py` as currently written (it assumes ct2-ready repos only). Never pass a non-ct2 HF repo straight to `WhisperModel` — always check the repo has `model.bin`+ct2 config files, not raw `pytorch_model.bin`.
- `iadisirza-x/*` (3 repos claiming Thai large-v3 ct2 fine-tunes) all failed differently: missing `model.bin`, corrupt ct2 metadata (`data did not match any variant of untagged enum ModelWrapper`), and symlink `WinError 1314` — unverified personal uploads, unreliable, do not retry.
- `Systran/faster-distil-whisper-large-v2` / `-v3` loaded fine but scored **0.0 accuracy** on Thai testset (0/20 matched) — distil-whisper distillation is English-only; garbage output on Thai despite successful load. Never use `faster-distil-whisper-*` for Thai, regardless of speed.
- `nvidia/nemotron-3.5-asr-streaming-0.6b`, `typhoon-ai/typhoon-asr-realtime`, `typhoon-ai/typhoon-asr-streaming-115m`, `typhoon-ai/typhoon-asr-streaming-nemotron-0.6b` — all failed: `Unable to open file 'model.bin'`. These are NeMo/Conformer-architecture models (streaming-native design), not Whisper/ct2 — fundamentally incompatible with `faster_whisper.WhisperModel`. Would require adding `nemo_toolkit` as a separate loader/dependency to use — real integration work, not a config swap. Do not retry via `WhisperModel`.
- `mesolitica/Malaysian-STT-Whisper`, `mesolitica/Malaysian-STT-Whisper-Stage2` — 404 `RepositoryNotFoundError`, repo IDs do not exist as given (web search summary was inaccurate). Not retried.

## Current Blockers
- **NEW, high priority, CONFIRMED on real audio**: Typhoon streaming ASR (`typhoon-ai/typhoon-asr-streaming-115m`) via NeMo loader beats Whisper `medium` on both speed (RTF 0.372 vs 0.81 on real 10-min noisy clip) and transcript quality (coherent vs garbled). Known defect: rare repetition-loop artifact (see Completed Tasks). NEXT SESSION: this is a strong candidate to replace faster-whisper as backend/asr.py's engine, but swap is nontrivial — different API (`ASRModel.transcribe()` vs `WhisperModel.transcribe()`), different streaming/chunking model needed for the live WebSocket mic path (backend currently does file-based chunk transcription, NeMo RNNT streaming has its own state-carry API for true incremental decoding). Needs its own scoped TASK.md — not a drop-in swap.
- `nemo_toolkit[asr]` install confirmed NOT to break faster-whisper stack — ran `python test_asr.py` post-install, faster_whisper imports/loads/transcribes fine. Output was FAIL (2/4 matched, `MATCHED: ['สวัสดี', 'ประชุม']`) but this is pre-existing: backend/asr.py is hardcoded to `"small"` model, and PROGRESS.md already recorded small model as 2/4 on this exact sample — not a new regression, just re-confirms known weak baseline. `gtts` click-version conflict still unverified in practice (not exercised by this test).
- backend/asr.py currently set to "small" model (deviated from the large-v3 verified in the ASR task — see Failed Attempts) because large-v3 and medium were both too slow on a real 10-min clip (56+ min / 30+ min CPU, still not done). NEXT SESSION: try `beam_size=1` (greedy decode, currently unset = faster-whisper default beam_size=5) on medium or large-v3 — suspect default beam_size=5 is the real cause of the slowdown on long/noisy real speech, not raw model size. If beam_size=1 fixes speed, re-verify accuracy against test_asr.py before trusting it for real use.
- **NEXT SESSION PRIORITY**: bench_asr.py results (5-sample gTTS testset) recommend swapping `backend/asr.py` model from `"small"` to `"medium"` — best min-max score (0.690), RTF 0.81 (realtime-capable, but narrow ~19% margin), accuracy 0.70 vs small's 0.30. NOT YET APPLIED to backend/asr.py — user has not confirmed the swap. Before applying: (1) get user confirmation (out of current TASK.md scope, which is report-only), (2) verify with `beam_size=1` against real long audio (`data/clip.wav`) since the gTTS testset is short/clean and does not represent real noisy/code-switched meeting speech at length — medium already failed real 10-min-clip speed once before (see above blocker).
- Real browser mic -> WebSocket flow not yet tested (no browser automation run). Only backend pipeline and server boot verified directly.
- Current README.md summary quality is rough (small model + code-switched casual speech = garbled transcript, weak mT5 summary) — see Completed Tasks row for detail. Worth re-running once beam_size tuning improves speed enough to afford a bigger model.
- faster-whisper/HF model downloads required one-time network access — accepted as setup-time exception (runtime stays offline after cache). small/medium/large-v3 whisper models + mT5_multilingual_XLSum all cached locally now.
- gTTS and yt-dlp (both one-time network calls, user-approved) used to fetch test/input audio — not part of runtime path.
