# test-chamber

Testing. Same scope as `trial-by-fire.md` — see that file for full detail (acceptance check,
benchmark scripts, untested live-flow gap). Kept as a separate named entry per project skill
index convention.
