# cipher-sanctum

Security. Same scope as `aegis-ward.md` — see that file for full detail (offline-only rule,
no-auth WebSocket caveat, secrets policy). Kept as a separate named entry per project skill
index convention.
