# migration-rites

Database migrations. **Not applicable yet** — no database exists (see `database-catacombs.md`).
Kept as a stub for future reference.

If a database gets added later:
- [ ] Pick a migration tool matching whatever DB layer gets chosen (e.g. `alembic` for SQLAlchemy)
- [ ] Never hand-edit schema in place once there's real data — migrations only, from day one
