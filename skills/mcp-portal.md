# mcp-portal

MCP integration. **Not applicable yet** — this project has no MCP server/client integration.
Kept as a stub for future reference.

If MCP integration ever gets added:
- [ ] Confirm it doesn't introduce a network dependency that violates `prime-directive.md` —
  local MCP servers only, unless the offline constraint is explicitly relaxed for that feature
- [ ] Clarify what an MCP server would even expose here — transcript search? triggering a
  summarize run from another tool?
