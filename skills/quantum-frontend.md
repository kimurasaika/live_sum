# quantum-frontend

Frontend. Same scope as `neon-forge-frontend.md` — see that file for full detail (mic-capture
flow, chunking, known gaps). Kept as a separate named entry per project skill index convention.
