# abyssal-api

Backend / API. Same scope as `iron-gate-backend.md` — see that file for full detail (routes,
WebSocket handler, known blocking bug, model config). Kept as a separate named entry per project
skill index convention.
